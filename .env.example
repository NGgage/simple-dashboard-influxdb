# InfluxDB Connection
# ====================

# InfluxDB API endpoint (required)
INFLUX_BASE_URL=http://localhost:8086/api/v2

# Bucket name to query data from (required)
INFLUX_BUCKET=my-bucket

# Organization name (required)
INFLUX_ORGANIZATION=my-org

# API token with read access to the bucket (required)
# Generate at: InfluxDB UI -> Data -> API Tokens -> Generate API Token
INFLUX_TOKEN=your-api-token-here

# Server Configuration
# ====================

# Port for the dashboard server (optional, default: 3000)
# PORT=3000
